<%= form_with(model: sound, local: true) do |form| %>
  <% if sound.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sound.errors.count, "error") %> prohibited this sound from being saved:</h2>

      <ul>
      <% sound.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type_of_sound %>
    <%= form.text_field :type_of_sound %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key %>
  </div>

  <div class="field">
    <%= form.label :tempo %>
    <%= form.text_field :tempo %>
  </div>

  <div class="field">
      <%= form.label :soundfile %>
      <%= form.file_field :soundfile %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
